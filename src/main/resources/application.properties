server.port=8585
server.servlet.context-path=/boot


########################################################
###datasource
########################################################
spring.datasource.driverClassName = org.postgresql.Driver
spring.datasource.url = jdbc:postgresql://10.0.17.170:5432/boottest
spring.datasource.username = web
spring.datasource.password = qxqis
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
#spring.datasource.max-active=20
#spring.datasource.max-idle=8
#spring.datasource.min-idle=8
#spring.datasource.initial-size=10

########################################################
### Java Persistence Api
########################################################
# Specify the DBMS
spring.jpa.database = POSTGRESQL
# Show or not log for each sql query
spring.jpa.show-sql = true
# Hibernate ddl auto (create：启动会删除表重建, create-drop, update：，none, validate)
spring.jpa.hibernate.ddl-auto = update
#org.postgresql.jdbc.PgConnection.createClob() 方法尚未被实作。
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

##mvc
spring.mvc.view.prefix=/WEB-INF
spring.mvc.view.suffix=.jsp

